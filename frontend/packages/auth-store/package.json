{
  "name": "@cloudpos/auth-store",
  "version": "0.1.0",
  "description": "Authentication store and utilities for CloudPOS applications",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit"
  },
  "peerDependencies": {
    "@reduxjs/toolkit": "^1.9.0",
    "react": "^18.0.0",
    "react-redux": "^8.0.0"
  },
  "dependencies": {
    "@cloudpos/types": "file:../types",
    "@cloudpos/api-client": "file:../api-client",
    "@cloudpos/utils": "file:../utils"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}