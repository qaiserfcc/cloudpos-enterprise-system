{
  "name": "@cloudpos/api-client",
  "version": "0.1.0",
  "description": "Shared API client for CloudPOS applications",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit"
  },
  "peerDependencies": {
    "@reduxjs/toolkit": "^1.9.0",
    "react": "^18.0.0"
  },
  "dependencies": {
    "@cloudpos/types": "file:../types"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@reduxjs/toolkit": "^1.9.0"
  }
}